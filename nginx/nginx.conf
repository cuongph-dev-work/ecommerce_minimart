upstream api {
    server api:8000;
}

upstream web {
    server web:80;
}

upstream admin {
    server admin:80;
}

upstream minio {
    server minio:9000;
}

# API - HTTPS
server {
    listen 443 ssl http2;
    server_name api.littlebox.vn;

    ssl_certificate /etc/letsencrypt/live/api.littlebox.vn/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/api.littlebox.vn/privkey.pem;
    
    # SSL configuration
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;
    ssl_prefer_server_ciphers on;
    ssl_session_cache shared:SSL:10m;
    ssl_session_timeout 10m;

    location / {
        proxy_pass http://api;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;

        # Increase timeouts for long-running requests
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }

    # Gzip compression
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_types text/plain text/css text/xml text/javascript application/x-javascript application/xml+rss application/javascript application/json;
}

# API - HTTP to HTTPS redirect
server {
    listen 80;
    server_name api.littlebox.vn;
    return 301 https://$server_name$request_uri;
}

# Admin - HTTPS
server {
    listen 443 ssl http2;
    server_name admin.littlebox.vn;

    ssl_certificate /etc/letsencrypt/live/admin.littlebox.vn/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/admin.littlebox.vn/privkey.pem;
    
    # SSL configuration
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;
    ssl_prefer_server_ciphers on;
    ssl_session_cache shared:SSL:10m;
    ssl_session_timeout 10m;

    location / {
        proxy_pass http://admin;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }

    # Gzip compression
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_types text/plain text/css text/xml text/javascript application/x-javascript application/xml+rss application/javascript application/json;
}

# Admin - HTTP to HTTPS redirect
server {
    listen 80;
    server_name admin.littlebox.vn;
    return 301 https://$server_name$request_uri;
}

# Web - HTTPS
server {
    listen 443 ssl http2;
    server_name littlebox.vn www.littlebox.vn;

    ssl_certificate /etc/letsencrypt/live/littlebox.vn/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/littlebox.vn/privkey.pem;
    
    # SSL configuration
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;
    ssl_prefer_server_ciphers on;
    ssl_session_cache shared:SSL:10m;
    ssl_session_timeout 10m;

    location / {
        proxy_pass http://web;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }

    # Gzip compression
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_types text/plain text/css text/xml text/javascript application/x-javascript application/xml+rss application/javascript application/json;
}

# Web - HTTP to HTTPS redirect
server {
    listen 80;
    server_name littlebox.vn www.littlebox.vn;
    return 301 https://$server_name$request_uri;
}

# Assets/MinIO - HTTPS
server {
    listen 443 ssl http2;
    server_name assets.littlebox.vn;

    ssl_certificate /etc/letsencrypt/live/assets.littlebox.vn/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/assets.littlebox.vn/privkey.pem;
    
    # SSL configuration
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;
    ssl_prefer_server_ciphers on;
    ssl_session_cache shared:SSL:10m;
    ssl_session_timeout 10m;

    location / {
        proxy_pass http://minio;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }

    # Gzip compression
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_types text/plain text/css text/xml text/javascript application/x-javascript application/xml+rss application/javascript application/json;
}

# Assets/MinIO - HTTP to HTTPS redirect
server {
    listen 80;
    server_name assets.littlebox.vn;
    return 301 https://$server_name$request_uri;
}
